# AgentProbe — Environment Variables
# Copy to .env:  cp .env.example .env

# Your Anthropic key (for the LLM-judge evaluator)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: OpenAI key (if customers test OpenAI agents)
OPENAI_API_KEY=sk-your-key-here

# PayFast (primary payment — South Africa)
PAYFAST_MERCHANT_ID=your-live-merchant-id
PAYFAST_MERCHANT_KEY=your-live-merchant-key
PAYFAST_PASSPHRASE=your-live-passphrase
PAYFAST_SANDBOX=false

# Stripe (fallback — for when you set up Stripe Atlas)
STRIPE_SECRET_KEY=sk_test_your-stripe-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret
STRIPE_PRO_PRICE_ID=price_xxxxx
STRIPE_ENTERPRISE_PRICE_ID=price_xxxxx

# Email alerts (for scheduled test failures)
# Use any SMTP provider: Gmail, SendGrid, Postmark, SES
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=alerts@agentprobe.dev

# App
DOMAIN=http://localhost:3000
API_DOMAIN=http://localhost:8000
ADMIN_SECRET=admin-change-me-in-production
